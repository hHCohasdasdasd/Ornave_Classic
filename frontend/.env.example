VITE_API_URL=http://localhost:3000/api
# Safe to expose client-side by design — this is Stripe's "publishable" key,
# not the secret one. Same value as backend/.env's STRIPE_PUBLISHABLE_KEY.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
